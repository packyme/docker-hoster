# Docker Hoster 配置文件
# 复制此文件为 .env 并根据需要自定义

# Hosts 文件路径（默认：/etc/hosts）
HOSTS_FILE=/etc/hosts

# Docker 守护进程 socket URL（默认：自动从环境检测）
# 示例：
#   unix:///var/run/docker.sock
#   tcp://127.0.0.1:2375
#DOCKER_HOST=unix:///var/run/docker.sock

# 启用容器标签过滤（默认：false）
# 启用后，只会处理带有指定标签的容器
ENABLE_LABEL_FILTER=false

# 过滤标签键（默认：hoster.enable）
LABEL_KEY=hoster.enable

# 过滤标签值（默认：true）
LABEL_VALUE=true

# 日志级别（默认：INFO）
# 选项：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
